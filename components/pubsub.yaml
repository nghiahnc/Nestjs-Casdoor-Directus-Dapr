apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: my-pubsub
spec:
  type: pubsub.redis
  version: v1
  metadata:
  - name: redisHost
    value: localhost:6379  # Nếu chưa có Redis, cài Redis local hoặc Docker Redis
  - name: redisPassword
    value: ""